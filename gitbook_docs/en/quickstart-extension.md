## Quickstart — Extension

1. Change to `extension`.
2. Install deps and build:
```
cd extension
npm install
npm run build
```
3. Load unpacked in Chrome: `chrome://extensions/` → Developer mode → Load unpacked.
