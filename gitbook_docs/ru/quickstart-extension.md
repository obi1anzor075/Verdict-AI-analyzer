## Быстрый старт — Расширение

1. Перейти в каталог `extension`.
2. Установить зависимости и собрать:
```
cd extension
npm install
npm run build
```
3. В Chrome: `chrome://extensions/` → Developer mode → Load unpacked → выбрать `extension` или `extension/dist`.
